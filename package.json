{
  "name": "@lifeup/mcp",
  "version": "0.1.0",
  "private": true,
  "description": "npx github wrapper for the LifeUp Cloud MCP in mcp/",
  "type": "module",
  "bin": {
    "lifeup-mcp": "./mcp/dist/index.js"
  },
  "scripts": {
    "prepare": "npm --prefix mcp ci && npm --prefix mcp run build"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Ayagikei/LifeUp-SDK.git"
  }
}
